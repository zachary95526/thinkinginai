server:
  port: 8080

spring:
  application:
    name: tia-alibaba-rag
  ai:
    openai:
      api-key: <changeit>
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-max
      embedding:
        options:
          model: text-embedding-v1
